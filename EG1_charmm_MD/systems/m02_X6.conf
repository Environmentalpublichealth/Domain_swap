# m02_X6 -- generated by new_system.sh on 2026-09-24
# Sourced by prep_system.sh AFTER config.sh: per-molecule facts only.

RECEPTOR_SRC=inputs/receptors/fold_m02_1ks5backbone_1xyninsert_model_0_clean.pdb
CHAIN=A
N_RESIDUES=223

# Checked against what pdb2gmx actually builds. If the build disagrees, the
# structure is not what this file says it is -- find out why before continuing.
EXPECT_DISULFIDES=0
EXPECT_HIS=2
EXPECT_CHARGE=-10

# 1 = a ligand is merged in from build/m02_X6/glycan.itp (built by
# 02_build_glycan.py from the docked pose). 0 = protein only.
HAS_LIGAND=1
LIGAND=X6
POSE_SRC=inputs/poses/fold_m02_1ks5backbone_1xyninsert_model_0_X6_ad4_out.pdbqt
POSE_INDEX=1
